import UIKit

final class SceneDelegate: UIResponder, UIWindowSceneDelegate {
    // The window is created by UIKit from the Main storyboard
    // (UISceneStoryboardFile in Info.plist).
    var window: UIWindow?
}
